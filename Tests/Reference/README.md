# Reference implementation

The files in `ft8_lib` are retained only as an independent parity reference.
They are excluded from the Swift package targets and are not shipped in the FT8Protocol library product.
See the included upstream licence.
